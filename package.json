{
  "name": "truck-insurance-quote",
  "version": "1.0.0",
  "scripts": {
    "dev": "NODE_OPTIONS='--openssl-legacy-provider' next dev",
    "build": "NODE_OPTIONS='--openssl-legacy-provider' next build",
    "start": "NODE_OPTIONS='--openssl-legacy-provider' next start"
  },
  "dependencies": {
    "googleapis": "^148.0.0",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
